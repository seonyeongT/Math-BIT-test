import React from "react";
import ReactDOM from "react-dom/client";
import MathPathQuiz from "./MathPathQuiz";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MathPathQuiz />
  </React.StrictMode>
);